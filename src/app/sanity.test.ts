import { describe, it, expect } from 'vitest';

// Simple passing test to confirm the test runner is wired up correctly
describe('sanity', () => {
  it('true is true', () => {
    expect(true).toBe(true);
  });
});